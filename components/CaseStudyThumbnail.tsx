/** Step 1: placeholder. Step 2 will replace with code-generated thumbnail. */
export function CaseStudyThumbnail({ id }: { id: string }) {
  return (
    <div className="flex w-full aspect-[16/10] items-center justify-center bg-neutral-900">
      <span className="font-mono text-sm text-neutral-600">{id}</span>
    </div>
  );
}
